/**
 * AI-Flux Design System — Modal / Dialog
 *
 * @example
 * import Modal, { ModalHeader, ModalBody, ModalFooter } from '@ai-flux/modal';
 * // or
 * import { Modal, ModalHeader, ModalBody, ModalFooter } from '@ai-flux/modal';
 */

export { Modal, ModalBody, ModalFooter, ModalHeader } from './Modal';
export type {
  DrawerPlacement,
  ModalBodyProps,
  ModalFooterProps,
  ModalHeaderProps,
  ModalProps,
  ModalSize,
  ModalVariant,
} from './Modal.types';
export { default } from './Modal';
