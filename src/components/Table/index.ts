export { Table, TablePagination } from './Table';

export type {
  TableProps,
  TableColumn,
  TableVariant,
  TableSize,
  SortDirection,
  TableSortState,
  TablePaginationState,
} from './Table';

export { default } from './Table';
