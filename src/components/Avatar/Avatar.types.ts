/**
 * AI-Flux Design System — Avatar Type Definitions
 * Re-exported for consumers who need only the types.
 */

export type {
  AvatarProps,
  AvatarSize,
  AvatarStatus,
  AvatarShape,
  AvatarGroupProps,
} from './Avatar';
